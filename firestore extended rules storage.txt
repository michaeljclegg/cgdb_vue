rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {

      allow read, write: if request.auth.token.roles.size() > 0;
      allow read, write: if
          request.time < timestamp.date(2023, 2, 20);
    }
  }
}